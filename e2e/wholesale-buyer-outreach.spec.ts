import { test } from '@playwright/test'

test.skip(true, 'Blocked by #289: inspecting fixture buyer outreach requires an authenticated reset-safe QA identity.')

test('investor inspects a linked buyer outreach history from a wholesale deal', async () => {})
