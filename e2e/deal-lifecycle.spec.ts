import { test } from '@playwright/test'

test('investor creates, saves, and deletes a deal with correlated audit evidence', async () => {
  test.skip(true, 'Blocked until the isolated QA fixture reset rehearsal in #289 is authorized and passes.')
})

test('investor creates a Tax Deed deal with correlated audit evidence', async () => {
  test.skip(true, 'Blocked until the isolated QA fixture reset rehearsal in #289 is authorized and passes.')
})

test('investor creates a Foreclosure deal with correlated audit evidence', async () => {
  test.skip(true, 'Blocked until the isolated QA fixture reset rehearsal in #289 is authorized and passes.')
})

test('investor traces tax-lien lifecycle saves with correlated audit evidence', async () => {
  test.skip(true, 'Blocked until the isolated QA fixture reset rehearsal in #289 is authorized and passes.')
})
